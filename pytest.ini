[pytest]
# Tests live in tests/ (keeps the repo root readable). `pythonpath = .` puts the
# repo root on sys.path so the suite can `import build` / `import sanity_check`
# from the subfolder; `testpaths` lets a bare `pytest` from the root find them.
testpaths = tests
pythonpath = .
